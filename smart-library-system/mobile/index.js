/**
 * Entry Point for React Native App
 * Registers the app with Expo
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the app component
registerRootComponent(App);
